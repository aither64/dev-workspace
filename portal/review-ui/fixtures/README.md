# Exact diff regression fixture

`oauth2.json` contains the unmodified before and after sources of
`api/lib/vpsadmin/api/authentication/oauth2_config.rb` from the vpsAdmin project.
The before revision is `5ac49802f892da6fd37a88c8349b9cc7ea826538`; the after
revision is `7159fec1390e04156836797cfae1206b99d71531`. These sources are covered
by the upstream GNU General Public License version 3, included in `COPYING`.
They are test data and are not bundled into the browser editor.

The stored zero-context Git ranges describe 116 additions and six deletions.
Both the repository reader and editor projections verify this comparison.
